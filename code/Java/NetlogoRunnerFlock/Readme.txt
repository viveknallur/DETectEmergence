This library is used to create a jar that can be used to run NetLogo Headless for both the flocking and pedestrian models.

To recompile, simply export the eclipse project as a runnable jar.

The makeRun file and runInput file are used in connection with this library.

makeRun - runs the jar created by this project
runInput - is read by the jar and used to assign the parameter values to that are used in each simulation run.
